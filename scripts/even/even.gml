/// @func even(num)
/// @desc Tests if the specified Real is even or not. Logically equivalent
///		  to checking if num%2 == 0.
///	@param {real} num the number to check
/// @return {bool} Returns true if the number is even, false otherwise
///
function even(num) {
	return num%2 == 0;
}